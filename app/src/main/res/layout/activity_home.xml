<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.unlimiteduniverse.cat.fetation.mvp.ui.home.HomeActivity">

    <FrameLayout
        android:id="@+id/content"
        android:layout_above="@id/navigation"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.BottomNavigationView
        android:id="@+id/navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="0dp"
        android:layout_marginStart="0dp"
        android:background="@color/bottom_nav_bg_color"
        android:layout_alignParentBottom="true"
        app:itemIconTint="@color/bottom_nav_view_menu_color"
        app:itemTextColor="@color/bottom_nav_view_menu_color"
        app:menu="@menu/navigation" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/navigation"
        android:layout_margin="@dimen/fab_margin"
        android:layout_alignParentEnd="true"
        app:srcCompat="@drawable/float_action_button_plus" />

</RelativeLayout>
